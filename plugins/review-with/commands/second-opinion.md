---
description: Get a second opinion — run the external-reviewer panel (Gemini, Copilot, Codex) on a diff and merge the findings.
---

Invoke the `review-with-everyone` skill (this is an alias for the panel review).

Interpret any arguments as **scope** and/or **roster** (PR/branch/`staged`/`unstaged`/files; an optional subset of codex/gemini/copilot). Default scope: current branch vs base. Follow the skill exactly.

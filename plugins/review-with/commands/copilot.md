---
description: Second-opinion code review from GitHub Copilot — ship a diff to the local copilot CLI and relay findings.
---

Invoke the `review-with-copilot` skill.

Interpret any arguments as **scope** — a PR number/URL, a branch name, `staged`, `unstaged`, or file path(s). Default: current branch vs base. Follow the skill exactly (flags, non-interactive, relay verbatim).

---
description: Show your GitHub PR dashboard — PRs you opened, are assigned to, are review-requested on, and your followed list.
---

Invoke the `pr-tasklist` skill to show the user's GitHub PR dashboard.

If the user passed extra arguments after `/prs` (e.g. `/prs setup`, `/prs reconfigure`, `/prs unfollow`), pass that intent through to the skill.
